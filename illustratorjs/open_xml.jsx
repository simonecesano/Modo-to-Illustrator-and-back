// This is a proof of concept to show how to open an XML file in Illustrator // and use XPath to select parts of itvar f = new File('some/xml/file.xml');     // get the filef.open();                                  // need to do this first before reading contentvar vbom = f.read();                       // read it invar vbomxml = new XML(vbom);               // parse as XMLvbomxml.xpath('//ColorWay[@id = "B34481"]//ColorPartList/Color/TargetPart/..') // select some nodes